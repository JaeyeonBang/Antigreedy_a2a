# Copy to .env and fill in. .env is gitignored — never commit real keys.

# OpenRouter API key — only needed for --backend openrouter (real-LLM gate/bench/probe).
# The mock backend (default) needs no key. Get one at https://openrouter.ai/keys
OPENROUTER_API_KEY=
